import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { SowTestRoutingModule } from './sow-test-routing.module';
// import { NumberDirective } from './only-number.directive';

@NgModule({
  declarations: [
    // NumberDirective
  ],
  imports: [
    CommonModule,
    SowTestRoutingModule
  ]
})
export class SowTestModule { }
